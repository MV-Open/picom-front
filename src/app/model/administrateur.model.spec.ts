import { AdministrateurModel } from './administrateur.model';

describe('AdministrateurModel', () => {
  it('should create an instance', () => {
    expect(new AdministrateurModel()).toBeTruthy();
  });
});
